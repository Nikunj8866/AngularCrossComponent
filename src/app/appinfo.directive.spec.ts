import { AppinfoDirective } from './appinfo.directive';

describe('AppinfoDirective', () => {
  it('should create an instance', () => {
    const directive = new AppinfoDirective();
    expect(directive).toBeTruthy();
  });
});
